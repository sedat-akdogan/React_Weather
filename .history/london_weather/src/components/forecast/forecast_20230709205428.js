import React from 'react'

const Forecast = ({data}) => {
    return (
        <div>
            <label className="title">Daily</label>
            Accor
        </div>
    );
};

export default Forecast;