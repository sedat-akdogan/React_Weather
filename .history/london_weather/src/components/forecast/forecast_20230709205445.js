import React from 'react'
im

const Forecast = ({data}) => {
    return (
        <div>
            <label className="title">Daily</label>
            <Accordion>

            </Accordion>
        </div>
    );
};

export default Forecast;